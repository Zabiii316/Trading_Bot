# Phase 16 — Micro Live Locked Template
# Real trading template. Do not use until manual approval and readiness gates pass.

export BINANCE_TESTNET=false
export BINANCE_USE_TESTNET=false

# Keep disabled by default
export BINANCE_ENABLE_LIVE_TRADING=false
export LIVE_TRADING_ALLOWED=false

# Real keys must NEVER be committed.
# Load real keys from a private .env file or secret manager only.
export BINANCE_API_KEY=do_not_commit_real_key
export BINANCE_API_SECRET=do_not_commit_real_secret
